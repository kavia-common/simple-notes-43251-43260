# simple-notes-43251-43260

A simple note-taking application allowing users to create, view, edit, and delete their notes.

- Frontend: Svelte (SvelteKit) in `note_frontend/`
- Run:
  ```bash
  cd note_frontend
  npm install
  npm run dev
  ```
- App URL (dev): http://localhost:3000